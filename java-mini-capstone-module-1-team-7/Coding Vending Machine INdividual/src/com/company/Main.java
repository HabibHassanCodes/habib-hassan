package com.company;

import java.util.HashMap;
import java.util.List;
import java.util.Map;

public class Main () {

    public void countInventory() {

        int totalCount = 5;


        for (int i = 0; i < totalCount; i++) {
            totalCount = totalCount - //userInput;
            if (totalCount == 0) {
                String soldOut = "Product is SOLD OUT.";
            }
            else if (totalCount < 0) {
                String notEnough = "The quantity chosen is invalid. Not enough in inventory.";
            } else {
                //insert dispense items and amount action method thing
            }

        }


        }
}
